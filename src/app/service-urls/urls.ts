import { environment } from "src/environments/environment";

export class Urls {
    
    public static OPPTYPE = `${environment.BASE_URL}type`;
    public static areaStandard = `${environment.BASE_URL}standard`;
    public static areaName = `${environment.BASE_URL}city`; 
    public static Tenant_Country = `${environment.BASE_URL}country`; 
    public static opportunity_name = `${environment.BASE_URL}OPPORTUNITY_NAME`;
    public static contract = `${environment.BASE_URL}contract`;
   
 }

